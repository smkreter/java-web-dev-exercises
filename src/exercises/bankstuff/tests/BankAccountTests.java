package exercises.bankstuff.tests;

import org.junit.Before;
import org.junit.Test;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.fail;
/*
public class BankAccountTests {
    private BankAccount testAccount;
    @Before
    BankAccount testAccount = new BankAccount(accountNumber);
    //bank accounts have
        //bank acct number
        //routing #
        //balance
        //owner or owners
        //type of bank account - checking, savings, etc
    //things bank accounts can do
        //deposit
    @Test
    public void testSimpleDeposit() {
        testAccount.deposit(1);
        assertEquals(1, testAccount.getBalance(), .001);
    }

    @Test(expected = IllegalAccessException.class)
    public void testDepositNegative() {
        testAccount.deposit(-2);
        fail("should not deposit a negative amount");
    }

        //withdraw
        //transfer
        //check balance
}

 */
